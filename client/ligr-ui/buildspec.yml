version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - npm run build

artifacts:
  base-directory: build
  files:
    - index.html
    - manifest.json
    - static/**/*  # Include all files in /static
